import { Montserrat, Poppins } from "next/font/google";

const monts = Montserrat({ weight: ["400"], subsets: ["latin"] });
const pops = Poppins({ weight: ["500"], subsets: ["latin"] });

export { monts, pops };
